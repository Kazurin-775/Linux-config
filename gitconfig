[alias]
	aa = add -A
	br = branch
	cf = config
	cl = clone
	cm = commit
	co = checkout
	di = diff
	ds = diff --staged
	puff = pull --ff-only
	scl = clone --depth=1
	st = status
	sm = submodule update --init
	smu = submodule update --init
	sw = switch
[core]
	quotepath = false
	# Windows-specific settings
	autocrlf = input
	safecrlf = warn
[pull]
	ff = only
